# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Claude Code — fully untracked as of 2026-08-21. .claude/settings.json + hooks/
# now sync via claude-config-sync instead of being tracked here (see
# DOCS_MIGRATION_PLAN.md in the config workspace) -- .gitignore doesn't need
# per-file exceptions anymore, the whole folder is ignored.
.claude/
.remember/

# Environment variables
.env
.env.*
!.env.example

# Process documentation — moved out to the private documentacion repo,
# symlinked back here for local viewing (see DOCS_MIGRATION_PLAN.md)
ARCHITECTURE.md
CLAUDE.md
MILESTONE_LOG.md
WORKLOG.md
TECH_LEARNING_LOG.md
design-handoff.md
discovery-brief.md
discovery-brief.html
discovery-brief.pdf
page-specs.md
typography-comparison.html
Screenshoot vesion worpress
